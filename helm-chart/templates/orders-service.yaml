apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.ordersService.name }}
  labels:
    app: {{ .Values.ordersService.name }}
spec:
  replicas: {{ .Values.ordersService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.ordersService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.ordersService.name }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.ordersService.port }}"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: {{ .Values.ordersService.name }}
        image: "{{ .Values.global.imageRegistry }}{{ .Values.ordersService.image }}:{{ .Values.global.imageTag }}"
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: {{ .Values.ordersService.port }}
          name: http
        resources:
          {{- toYaml .Values.ordersService.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.ordersService.port }}
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.ordersService.port }}
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ordersService.name }}
  labels:
    app: {{ .Values.ordersService.name }}
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "{{ .Values.ordersService.port }}"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.ordersService.port }}
    targetPort: {{ .Values.ordersService.port }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.ordersService.name }}
