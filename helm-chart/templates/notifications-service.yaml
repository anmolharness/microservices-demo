apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.notificationsService.name }}
  labels:
    app: {{ .Values.notificationsService.name }}
spec:
  replicas: {{ .Values.notificationsService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.notificationsService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.notificationsService.name }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.notificationsService.port }}"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: {{ .Values.notificationsService.name }}
        image: "{{ .Values.global.imageRegistry }}{{ .Values.notificationsService.image }}:{{ .Values.global.imageTag }}"
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: {{ .Values.notificationsService.port }}
          name: http
        resources:
          {{- toYaml .Values.notificationsService.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.notificationsService.port }}
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.notificationsService.port }}
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.notificationsService.name }}
  labels:
    app: {{ .Values.notificationsService.name }}
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "{{ .Values.notificationsService.port }}"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.notificationsService.port }}
    targetPort: {{ .Values.notificationsService.port }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.notificationsService.name }}
